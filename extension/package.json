{
  "name": "conscious-media-extension",
  "version": "1.0.0",
  "description": "A sleek, minimalist, dark-themed Chrome extension that drives conscious media consumption",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "npm run clean && npm run copy-files",
    "copy-files": "cp manifest.json dist/ && cp -r icons dist/ && cp -r src/background dist/src/ && cp -r src/content dist/src/ && cp -r src/popup dist/ && cp -r src/dashboard dist/",
    "dev": "npm run build",
    "test": "echo 'Tests would run here - skipping for production build'",
    "test:manual": "echo 'Manual testing required for Chrome extension'",
    "lint": "echo 'Linting skipped for production build'",
    "package": "npm run build && npm run zip",
    "zip": "cd dist && zip -r ../conscious-media-extension.zip .",
    "clean": "rm -rf dist && mkdir -p dist"
  },
  "keywords": [
    "chrome-extension",
    "media-consumption",
    "time-tracking",
    "productivity",
    "focus"
  ],
  "author": "Manus AI",
  "license": "MIT",
  "dependencies": {
    "idb-keyval": "^6.2.1",
    "uuid": "^9.0.1"
  }
}

